---
name: tiantan-general-word-format
description: 使用"天坛家具通用公文格式"创建或改写正式Word文件。适用于公司内部汇报、项目汇报、专题汇报、会议纪要及其他正式材料；沿用参考文件的页面设置、标题层级、字体字号、固定行距和段落样式。
---

# 天坛家具通用公文格式

使用本 Skill 为天坛家具创建符合通用公文格式的正式 Word 文档。

## 工作流程

1. 读取 `${CLAUDE_SKILL_DIR}/assets/reference.docx`，将其作为唯一版式基准。
2. 复制参考文件并替换正文内容，不修改参考文件本身。
3. 保留页面设置、标题层级、字体字号、固定行距、段落间距和页面元素。
4. 根据用户提供的材料撰写内容；不得为填充模板而编造事实。
5. 生成新的 `.docx` 文件，交付前检查标题、正文、落款、分页是否完整。

## 输出模式

### 默认模式（无附件）

直接以主标题开头，无需"附件"标签。正文末尾加落款和联系人。

```
主标题                          → 小二号方正小标宋简体，居中
  └ 正文                        → 三号仿宋_GB2312，行距28pt，首行缩进2字符
  └ 一、一级标题                 → 三号黑体
      └ （一）二级标题           → 三号楷体_GB2312
          └ 1.三级标题          → 三号仿宋_GB2312
  └ 落款（发文单位 + 日期）
  └ 联系人
```

### 附件模式（用户明确要求时才启用）

附件说明、落款、联系人**仅出现在正文末尾**，附件内部不加。

```
[正文末尾]
  附件说明
  落款
  联系人

[附件正文 — 分页符新页]
  附件X                          → 三号黑体，左对齐
    └ 主标题                     → 小二号方正小标宋简体，居中
        └ 正文 + 各级标题        → 同默认模式
        └ （无落款、无附件说明、无联系人）

[下一附件 — 分页符新页]
  附件Y                          → 同上
```

**附件规则：**
- 多个附件之间用分页符分隔（`w:br w:type="page"`）。
- 附件名称末尾不加标点符号。
- 附件说明回行时与首行附件名称文字对齐。

## 格式规范总表

### 页面

| 属性 | 值 |
|------|-----|
| 纸张 | A4（210mm × 297mm），纵向 |
| 上边距 | 25.4mm |
| 下边距 | 25.4mm |
| 左边距 | 28mm |
| 右边距 | 26mm |

### 字体字号

| 元素 | 字体 | 字号 | 对齐 | 缩进/间距 |
|------|------|------|------|-----------|
| 附件标签 | 黑体 | 三号 16pt | 左对齐 | 段后 7.8pt |
| 主标题 | 方正小标宋简体 | 小二号 18pt | **居中** | — |
| 一级标题 | 黑体 | 三号 16pt | 两端对齐 | 首行缩进 2 字符 |
| 二级标题 | 楷体\_GB2312 | 三号 16pt | 两端对齐 | 首行缩进 2 字符 |
| 三级标题 | 仿宋\_GB2312 | 三号 16pt | 两端对齐 | 首行缩进 2 字符 |
| 正文 | 仿宋\_GB2312 | 三号 16pt | 两端对齐 | 首行缩进 2 字符 |
| 附件说明 | 仿宋\_GB2312 | 三号 16pt | 两端对齐 | 首行缩进 2 字符 |
| 发文单位 | 仿宋\_GB2312 | 三号 16pt | 右对齐 | **右空 2 字符** |
| 成文日期 | 仿宋\_GB2312 | 三号 16pt | 右对齐 | 以发文单位名为基准居中 |
| 联系人 | 仿宋\_GB2312 | 三号 16pt | 左对齐 | **首行左空 2 字符**，续行顶格 |

### 段落

| 属性 | 值 |
|------|-----|
| 固定行距 | **28pt**（28 磅，固定值） |
| 正文样式 | 基于 "Body Text Indent" |
| 首行缩进 | 2 字符 |
| 正文对齐 | 两端对齐 |

### 落款

- 位置：正文或附件说明结束后**空一行**。
- 发文单位在上，成文日期在下，两者之间不空行。
- 日期横向中心与发文单位横向中心对齐（非各自独立右对齐）。
- 日期格式：阿拉伯数字，如 `2026年8月3日`。
- 默认值：发文单位 = `北京金隅天坛家具股份有限公司`，日期 = 当天。
- **仅正文末尾添加**，附件内不加。

### 联系人

- 位置：落款结束后**空一行**。
- 写法：`（联系人：×××；联系电话：×××××）`。
- 首行左空 2 字符，回行时第二行居左不空格。
- 如用户未提供，可省略。
- **仅正文末尾添加**，附件内不加。

### 附件说明（有附件时）

- 位置：正文结束后空一行，落款之前。
- 写法：`附件：1.标题一`，名称末尾不加标点。
- 多个附件分行列出，回行对齐首行附件名称文字。
- **仅正文末尾添加**，附件内不加。

## 中文标点规范

| 类型 | ✅ 正确 | ❌ 错误 |
|------|---------|---------|
| 双引号 | `""`（U+201C/D） | `""` 或 `「」` |
| 单引号 | `''`（U+2018/9） | `''` |
| 书名号 | `《》` | `< >` |
| 句号 | `。` | `.` |
| 逗号 | `，` | `,` |
| 顿号 | `、` | 误用逗号 |
| 括号 | `（）` | `()` |
| 冒号 | `：` | `:` |
| 分号 | `；` | `;` |

- 正文标点一律全角。
- 引用文件法规使用书名号 `《》`。
- 数字字母与中文之间不加空格。

## 约束

- 用户未要求改变版式时，沿用参考文件样式，不自行设计。
- 表格遵循正式公文风格：黑色边框、列宽一致、字体对齐统一。
- 参考文件 `assets/reference.docx` 不得修改。
- 字体文件（FZXBSJW.ttf、simfang-仿宋GB.ttf、楷体\_GB2312.ttf）存放于 `assets/`。
